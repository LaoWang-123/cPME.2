% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialization_volume_qc.R
\name{select_initial_pme_volume_qc}
\alias{select_initial_pme_volume_qc}
\title{Select an initial subject PME using orientation, proxy energy, and volume QC}
\usage{
select_initial_pme_volume_qc(
  template_pme,
  template_initialization,
  subject_data,
  volume_data,
  partition_plane,
  part_label = c("part1", "part2"),
  target_volume,
  n_trials = 100L,
  volume_rel_error_threshold = 0.5,
  init_args = list(d = 2),
  pme_args = list(d = 2, lambda = exp(-15:5), max_iter = 100, verbose = FALSE,
    print_plots = FALSE),
  volume_n_points = 5000L,
  volume_seed = NULL,
  init_seed = NULL,
  proxy_energy_grid = 10L,
  orientation_pca_source = "all_centers",
  volume_multiplier = 1,
  limit_scaler = 0.05,
  ref = NULL,
  data_max = NULL,
  verbose = FALSE
)
}
\arguments{
\item{template_pme}{Template PME fit.}

\item{template_initialization}{Template initialization object.}

\item{subject_data}{Subject partition point cloud used to fit PME.}

\item{volume_data}{Full ROI point cloud used for single-part volume
estimation.}

\item{partition_plane}{Partition plane with \code{center} and \code{normal}.}

\item{part_label}{Either \code{"part1"} or \code{"part2"}.}

\item{target_volume}{Target single-part volume in output units.}

\item{n_trials}{Number of initialization trials.}

\item{volume_rel_error_threshold}{Relative volume error threshold.}

\item{init_args}{Arguments passed to \code{initialize_pme()}.}

\item{pme_args}{Arguments passed to \code{pme()}.}

\item{volume_n_points}{Number of Monte Carlo points for volume QC.}

\item{volume_seed}{Optional base seed for volume estimates.}

\item{init_seed}{Optional seed before initialization generation.}

\item{proxy_energy_grid}{Number of grid points per parameter direction for
proxy energy.}

\item{orientation_pca_source}{Passed to \code{check_pme_orientation()}.}

\item{volume_multiplier}{Unit multiplier for volume estimates.}

\item{limit_scaler}{Sampling-box padding for volume estimation.}

\item{ref}{Optional reference point for volume classification.}

\item{data_max}{Optional sampling-box scale.}

\item{verbose}{Logical.}
}
\value{
A list containing the selected initialization, selected PME,
  selected summary row, all candidate rows, and selection metadata.
}
\description{
This function runs repeated subject PME initializations, filters mirror-
reversed parameterizations relative to a template initialization, ranks the
surviving candidates by a registration proxy energy, and selects the first
proxy-energy-ranked candidate whose single-part surface-volume relative
error is no larger than \code{volume_rel_error_threshold}. If no candidate
passes the threshold, the candidate with the smallest volume relative error
is selected and flagged by \code{selection_rule}.
}
