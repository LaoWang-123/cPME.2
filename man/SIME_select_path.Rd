% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SIME.R
\name{SIME_select_path}
\alias{SIME_select_path}
\title{Select eta for one or more threshold multipliers along one SIME path}
\usage{
SIME_select_path(
  f1_fun,
  f2_fun,
  data2,
  c = 1.3,
  eta_vec = c(exp(-15:-2), 0.1, exp(-1:5)),
  lambda = exp(-15:5),
  d = 2,
  epsilon = 1,
  max_iter = 100,
  SSD_ratio_threshold = 5,
  init_args_f2 = list(min_clusters = 10, alpha = 0.01, max_clusters = 100, algorithm =
    "isomap", rescale = FALSE, component_type = "centers", subsample_size = 5, d = 2),
  seed = NULL,
  output_mode = c("compact", "full"),
  shrinkage_grid = NULL,
  shrinkage_method = c("data_projection", "grid"),
  shrinkage_grid_name = NULL,
  verbose = TRUE
)
}
\arguments{
\item{f1_fun}{Reference manifold function.}

\item{f2_fun}{Baseline target manifold function.}

\item{data2}{Raw target data matrix (n x D).}

\item{c}{Positive threshold multiplier(s). Values are evaluated increasingly.}

\item{eta_vec}{Candidate eta values. Will be sorted increasingly.}

\item{lambda}{Candidate lambda values for \code{SIME()}.}

\item{d}{Intrinsic dimension.}

\item{epsilon, max_iter, SSD_ratio_threshold}{Same as in \code{SIME()}.}

\item{init_args_f2}{Arguments passed to \code{initialize_pme()}.}

\item{seed}{Optional random seed.}

\item{output_mode}{Output size mode. \code{"compact"} keeps only selected
unique eta fits; \code{"full"} keeps every evaluated eta fit.}

\item{shrinkage_grid}{Parameter grid used for shrinkage. If \code{NULL},
a 40 by 40 disk grid from \code{make_uv_grid()} is used.}

\item{shrinkage_method}{Method used to evaluate shrinkage.
\code{"data_projection"} is the default and
projects all rows of \code{data2} onto \code{f2_fun} and compares
manifolds at those projected parameter locations. \code{"grid"} compares
manifolds on \code{shrinkage_grid}.}

\item{shrinkage_grid_name}{Optional label for a user-supplied
\code{shrinkage_grid}. If omitted, grid shrinkage is labeled
\code{"default_disk_40x40"} for the default grid or \code{"user_grid"} for
a supplied grid.}

\item{verbose}{Logical.}
}
\value{
A list with \code{summary}, \code{eta_path}, and \code{fits}. In
  compact mode, \code{fits} stores only selected unique eta fits. In full
  mode, \code{fits} stores every evaluated eta fit. The top-level
  \code{shrinkage_method} records whether shrinkage used
  \code{"data_projection"} or a named grid.
}
\description{
This is an experimental multi-\code{c} wrapper for \code{SIME_select()}.
For a single \code{c}, it returns the same core fields as \code{SIME_select()}
and adds optional shrinkage metrics. For multiple \code{c} values, it fits
each eta at most once and reuses the fitted path when moving from smaller to
larger thresholds.
}
