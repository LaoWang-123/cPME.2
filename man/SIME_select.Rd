% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SIME.R
\name{SIME_select}
\alias{SIME_select}
\title{Select eta by threshold rule with early stopping}
\usage{
SIME_select(
  f1_fun,
  f2_fun,
  data2,
  c = 1.3,
  eta_vec = c(exp(-15:-2), 0.1, exp(-1:5)),
  lambda = exp(-15:5),
  d = 2,
  epsilon = 1,
  max_iter = 100,
  SSD_ratio_threshold = 5,
  init_args_f2 = list(min_clusters = 10, alpha = 0.01, max_clusters = 100, algorithm =
    "isomap", rescale = FALSE, component_type = "centers", subsample_size = 5, d = 2),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{f1_fun}{Reference manifold function (e.g. MRI PME embedding map).}

\item{f2_fun}{Baseline target manifold function (e.g. PET PME embedding map).}

\item{data2}{Raw target data matrix (n x D).}

\item{c}{Positive multiplier for threshold. threshold = c * baseline_msd.}

\item{eta_vec}{Candidate eta values. Will be sorted increasingly.}

\item{lambda}{Candidate lambda values for SIME().}

\item{d}{Intrinsic dimension.}

\item{epsilon, max_iter, SSD_ratio_threshold}{Same as in SIME().}

\item{init_args_f2}{Arguments passed to initialize_pme().}

\item{seed}{Optional random seed.}

\item{verbose}{Logical.}
}
\value{
A list containing the baseline MSD, threshold, eta path,
  selected eta, and selected SIME fit.
}
\description{
Fit SIME along an increasing eta grid. Use the baseline MSD from f2_fun
as a reference scale, define threshold = c * baseline_msd, and select
the largest eta whose fitted SIME MSD does not exceed the threshold.
}
\details{
Early stopping rule:
stop as soon as the fitted MSD exceeds the threshold.
}
