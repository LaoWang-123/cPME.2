% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface_preprocessing.R
\name{estimate_midplane}
\alias{estimate_midplane}
\title{Estimate a Midplane From Surface Coordinates}
\usage{
estimate_midplane(
  surface_points,
  coord_cols = c("x", "y", "z"),
  normal_pc = 3,
  center = c("mean", "median"),
  reference_normal = NULL
)
}
\arguments{
\item{surface_points}{Data frame or matrix containing 3D points.}

\item{coord_cols}{Coordinate columns to use when `surface_points` is a data
frame.}

\item{normal_pc}{Which PCA direction is used as the plane normal. PC1 cuts
across the longest axis, PC2 across the second axis, and PC3 is the
least-variance direction.}

\item{center}{Either `"mean"` or `"median"`.}

\item{reference_normal}{Optional numeric length-3 vector used to make the
normal direction sign consistent across template/subjects.}
}
\value{
A list with center, unit normal, plane coefficients, PCA rotation,
  standard deviations along PCs, and signed distances for the input points.
}
\description{
The plane is defined as `normal' * (point - center) = 0`. The normal is
chosen from a PCA/SVD direction of the point cloud, making the rule reusable
for both template and subject shapes.
}
