% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialization_volume_qc.R
\name{select_initial_pme_pair_volume_qc}
\alias{select_initial_pme_pair_volume_qc}
\title{Select paired initial PME fits with normal-ray volume QC}
\usage{
select_initial_pme_pair_volume_qc(
  template_part1,
  template_part2,
  subject_part1_data,
  subject_part2_data,
  volume_data,
  partition_plane,
  target_volumes,
  n_trials = 100L,
  volume_rel_error_threshold = 0.5,
  init_seed_part1 = NULL,
  init_seed_part2 = NULL,
  volume_seed = NULL,
  volume_seed_offsets = c(part1 = 10000, part2 = 20000),
  init_args = list(d = 2),
  pme_args = list(d = 2, lambda = exp(-15:5), max_iter = 100, verbose = FALSE,
    print_plots = FALSE),
  volume_n_points = 5000L,
  volume_multiplier = 1,
  limit_scaler = 0.05,
  proxy_energy_grid = 10L,
  orientation_pca_source = "all_centers",
  grid_n = 75L,
  ray_tol = 0.45,
  ray_min_t = 0.05,
  max_passes = 2L,
  verbose = FALSE
)
}
\arguments{
\item{template_part1, template_part2}{Template partition objects. Each object
must contain \code{pme1} and \code{initialization_f1}.}

\item{subject_part1_data, subject_part2_data}{Subject partition point clouds.}

\item{volume_data}{Full ROI point cloud used for volume estimation.}

\item{partition_plane}{Partition plane with \code{center} and \code{normal}.}

\item{target_volumes}{Named numeric vector or list with \code{part1} and
\code{part2} target volumes.}

\item{n_trials}{Number of initialization trials per part.}

\item{volume_rel_error_threshold}{Relative volume error threshold.}

\item{init_seed_part1, init_seed_part2}{Optional initialization seeds.}

\item{volume_seed}{Optional base seed for paired volume estimates.}

\item{volume_seed_offsets}{Named or length-2 numeric vector giving part1 and
part2 seed offsets.}

\item{init_args}{Arguments passed to \code{initialize_pme()}.}

\item{pme_args}{Arguments passed to \code{pme()}.}

\item{volume_n_points}{Number of Monte Carlo candidate points for volume QC.}

\item{volume_multiplier}{Unit multiplier for volume estimates.}

\item{limit_scaler}{Sampling-box padding for volume estimation.}

\item{proxy_energy_grid}{Number of grid points per parameter direction for
PME-based proxy energy.}

\item{orientation_pca_source}{Passed to \code{check_pme_orientation()}.}

\item{grid_n}{Number of parameter-grid points per direction used to seed
normal-ray intersection search.}

\item{ray_tol}{Maximum perpendicular distance for accepting a ray hit.}

\item{ray_min_t}{Minimum distance along the ray before a hit is treated as a
crossing rather than a partition-plane boundary contact.}

\item{max_passes}{Number of alternating paired refinement passes.}

\item{verbose}{Logical.}
}
\value{
A list with refined \code{part1}, refined \code{part2}, and combined
  \code{candidates}. Each part contains all candidate objects and the selected
  initialization/PME.
}
\description{
This is the preferred initialization selector for two-part real-data
workflows. It generates orientation-valid PME candidates and PME-based proxy
energy rankings for part 1 and part 2, then selects the final pair using
paired normal-ray volume QC. Proxy energy remains the primary ranking
criterion; volume is used as a guardrail to reject implausible candidates.
}
