% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialization_volume_qc.R
\name{refine_initial_pme_pair_normal_ray_qc}
\alias{refine_initial_pme_pair_normal_ray_qc}
\title{Refine paired initial PME choices with normal-ray volume QC}
\usage{
refine_initial_pme_pair_normal_ray_qc(
  init_qc_part1,
  init_qc_part2,
  volume_data,
  partition_plane,
  volume_n_points = 5000L,
  volume_seed = NULL,
  volume_seed_offsets = c(part1 = 10000, part2 = 20000),
  volume_multiplier = 1,
  limit_scaler = 0.05,
  grid_n = 75L,
  ray_tol = 0.45,
  ray_min_t = 0.05,
  max_passes = 2L
)
}
\arguments{
\item{init_qc_part1}{Output from \code{select_initial_pme_volume_qc()} for
part 1, saved with \code{save_candidate_objects = TRUE}.}

\item{init_qc_part2}{Output from \code{select_initial_pme_volume_qc()} for
part 2, saved with \code{save_candidate_objects = TRUE}.}

\item{volume_data}{Full ROI point cloud used for volume estimation.}

\item{partition_plane}{Partition plane with \code{center} and \code{normal}.}

\item{volume_n_points}{Number of Monte Carlo candidate points.}

\item{volume_seed}{Optional base seed for volume estimates.}

\item{volume_seed_offsets}{Named or length-2 numeric vector giving part1 and
part2 seed offsets.}

\item{volume_multiplier}{Unit multiplier for volume estimates.}

\item{limit_scaler}{Sampling-box padding for volume estimation.}

\item{grid_n}{Number of parameter-grid points per direction used to seed
normal-ray intersection search.}

\item{ray_tol}{Maximum perpendicular distance for accepting a ray hit.}

\item{ray_min_t}{Minimum distance along the ray before a hit is treated as a
crossing rather than a partition-plane boundary contact.}

\item{max_passes}{Number of alternating refinement passes. Two is usually
enough because part 2 can use the updated part 1 choice in the first pass.}
}
\value{
A list with refined \code{part1}, refined \code{part2}, and a
  combined \code{candidates} data frame.
}
\description{
Re-select two partition initial PME fits after both parts have already been
initialized and proxy-energy ranked. Each part is scanned in increasing proxy
energy order, and the first candidate passing single-part normal-ray volume
QC is selected. The other part's current selected PME is used as the
counterpart surface for normal-ray intersection-order diagnostics.
}
