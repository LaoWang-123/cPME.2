% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation Utils.R
\name{make_bump_field}
\alias{make_bump_field}
\title{Contamination field defined by a sum of Gaussian bumps (z-axis deformation)}
\usage{
make_bump_field(A, u0, v0, sigma)
}
\arguments{
\item{A}{Numeric vector of amplitudes \eqn{A_k}. Can contain both positive
and negative values to represent upward or downward deformation.}

\item{u0}{Numeric vector of bump centers in the \eqn{u}-direction.}

\item{v0}{Numeric vector of bump centers in the \eqn{v}-direction.}

\item{sigma}{Numeric vector of positive bandwidth parameters \eqn{\sigma_k},
controlling the spatial spread of each bump.}
}
\value{
A function \code{f(u, v)} that evaluates the contamination field.
}
\description{
This function constructs a smooth contamination vector field
\eqn{c : [0,1]^2 \to \mathbb{R}^3} acting along the z-axis,
defined as a linear combination of Gaussian radial basis functions:
\deqn{
h(u,v) = \sum_{k=1}^K A_k \exp\left(
-\frac{(u - u_k)^2 + (v - v_k)^2}{2\sigma_k^2}
\right).
}
}
\details{
This construction is commonly used to model structured contamination.
}
\examples{
# Two bumps: one positive, one negative
A     <- c(0.15, -0.12)
u0    <- c(0.3, 0.7)
v0    <- c(0.4, 0.6)
sigma <- c(0.15, 0.2)

contam_fn <- make_bump_field(A, u0, v0, sigma)

}
