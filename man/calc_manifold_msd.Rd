% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation Utils.R
\name{calc_manifold_msd}
\alias{calc_manifold_msd}
\title{Compute manifold approximation error (MSD) with respect to a true surface}
\usage{
calc_manifold_msd(
  f_hat,
  f_true,
  uv_grid,
  seed = 123,
  init_method = "pca",
  return_xyz = FALSE
)
}
\arguments{
\item{f_hat}{A function representing the estimated manifold \eqn{\hat f},
mapping from parameter space \eqn{U} to \eqn{\mathbb{R}^3}.}

\item{f_true}{A function representing the true manifold \eqn{f_{\text{true}}},
mapping from parameter space \eqn{U} to \eqn{\mathbb{R}^3}.}

\item{uv_grid}{A matrix or data frame of parameter values (u, v) used to
generate samples from the true manifold.}

\item{seed}{Integer seed for reproducibility when generating data.}

\item{init_method}{Character string specifying the initialization method
for projection (passed to \code{pme_initial_guess}). Default is \code{"pca"}.}

\item{return_xyz}{default FALSE}
}
\value{
A numeric scalar representing the mean squared distance (MSD)
}
\description{
This function computes the mean squared distance (MSD) between a true
manifold \eqn{f_{\text{true}}} and an estimated manifold \eqn{\hat f}.
The metric is defined as
\deqn{
\mathrm{MSD}(\hat f) = \mathbb{E} \left\| X - \hat f(\pi_{\hat f}(X)) \right\|^2,
}
}
