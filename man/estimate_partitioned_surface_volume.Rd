% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Evaluation_Utils.R
\name{estimate_partitioned_surface_volume}
\alias{estimate_partitioned_surface_volume}
\title{Estimate Volume From Two Fitted Surface Pieces}
\usage{
estimate_partitioned_surface_volume(
  fit_part1,
  fit_part2,
  data,
  n_points = 10000,
  limit_scaler = 0.05,
  partition_index = 3,
  partition_plane = NULL,
  ref = c(0, 0, 0),
  data_max = NULL,
  volume_multiplier = 1,
  classification_method = c("partition_side", "both_surfaces"),
  seed = NULL,
  fail_on_error = FALSE,
  warn_on_failure = TRUE
)
}
\arguments{
\item{fit_part1}{Fitted PME object or `SIME_select()` result for the first
surface piece.}

\item{fit_part2}{Fitted PME object or `SIME_select()` result for the second
surface piece.}

\item{data}{Numeric matrix/data frame with at least three columns giving the
point cloud used to define the candidate sampling box.}

\item{n_points}{Number of Monte Carlo candidate points.}

\item{limit_scaler}{Proportional padding added to each side of the data
bounding box.}

\item{partition_index}{Coordinate index used to split candidates when
`partition_plane` is `NULL`.}

\item{partition_plane}{Optional list with `center` and `normal` fields. If
supplied, candidates are assigned to part 1 or part 2 by signed distance to
this plane.}

\item{ref}{A known interior reference point in the same coordinate system as
`data`.}

\item{data_max}{Optional numeric length-3 scale. If supplied, candidates are
sampled from `[-data_max, data_max]` rather than the data bounding box.}

\item{volume_multiplier}{Multiplier applied to the estimated volume, for
example voxel volume when coordinates are voxel indices.}

\item{classification_method}{Either `"partition_side"` or `"both_surfaces"`.
`"partition_side"` classifies each candidate using only the surface piece
on its partition side. `"both_surfaces"` requires the candidate to be
inside both fitted pieces.}

\item{seed}{Optional random seed for candidate sampling.}

\item{fail_on_error}{Logical. If \code{FALSE}, invalid or missing fits return
an NA volume with a \code{volume_log} entry instead of stopping.}

\item{warn_on_failure}{Logical. If \code{TRUE}, non-fatal NA-volume failures
also emit a warning.}
}
\value{
A list containing the estimated `volume`, candidate points, interior
  indicators, sampling bounds, metadata, and detected fit types.
}
\description{
Estimate the volume enclosed by a shape represented by two fitted PME/SIME
surface pieces. Candidate 3D points are sampled from a padded bounding box
around `data`, classified as interior by surface normal orientation, and
converted to volume by Monte Carlo integration.
}
\details{
The inputs `fit_part1` and `fit_part2` can be cPME.2 PME objects or
`SIME_select()` results. For a `SIME_select()` result, the selected SIME fit
is used automatically.
}
