% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Evaluation_Utils.R
\name{calc_correspondence_msd}
\alias{calc_correspondence_msd}
\title{Compute Mean Squared Distance Between Two Manifolds Under Parameter Correspondence}
\usage{
calc_correspondence_msd(f_ref, f_cmp, uv_grid, seed = 123)
}
\arguments{
\item{f_ref}{A function representing the reference manifold. It should accept
input in \code{uv_grid} format and return an \eqn{n \times 3} matrix of 3D coordinates.}

\item{f_cmp}{A function representing the comparison manifold (e.g., SIME or PME estimate).
Same interface as \code{f_ref}.}

\item{uv_grid}{A data frame or matrix of parameter locations with columns \code{u} and \code{v}.}

\item{seed}{Integer random seed for reproducibility (passed to data generation).}
}
\value{
A numeric value representing the mean squared distance between the two manifolds.
}
\description{
This function computes the mean squared distance (MSD) between two manifold
functions evaluated on a shared parameter grid \code{uv_grid}. It assumes that
both manifolds are defined on the same parameter domain and that pointwise
correspondence is given by identical \code{(u, v)} locations.
}
