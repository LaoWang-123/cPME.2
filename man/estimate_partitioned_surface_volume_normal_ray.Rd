% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Evaluation_Utils.R
\name{estimate_partitioned_surface_volume_normal_ray}
\alias{estimate_partitioned_surface_volume_normal_ray}
\title{Estimate Volume With Normal-Ray Orientation Correction}
\usage{
estimate_partitioned_surface_volume_normal_ray(
  fit_part1,
  fit_part2,
  data,
  n_points = 10000,
  limit_scaler = 0.05,
  partition_plane,
  volume_multiplier = 1,
  seed = NULL,
  grid_n = 75L,
  ray_tol = 0.45,
  ray_min_t = 0.05,
  fail_on_error = FALSE
)
}
\arguments{
\item{fit_part1}{Fitted PME object or `SIME_select()` result for the first
surface piece.}

\item{fit_part2}{Fitted PME object or `SIME_select()` result for the second
surface piece.}

\item{data}{Numeric matrix/data frame with at least three columns giving the
point cloud used to define the candidate sampling box.}

\item{n_points}{Number of Monte Carlo candidate points.}

\item{limit_scaler}{Proportional padding added to each side of the data
bounding box.}

\item{partition_plane}{Optional list with `center` and `normal` fields. If
supplied, candidates are assigned to part 1 or part 2 by signed distance to
this plane.}

\item{volume_multiplier}{Multiplier applied to the estimated volume, for
example voxel volume when coordinates are voxel indices.}

\item{seed}{Optional random seed for candidate sampling.}

\item{grid_n}{Number of parameter-grid points per direction used to seed
ray-surface intersection search.}

\item{ray_tol}{Maximum perpendicular distance for accepting a ray hit.}

\item{ray_min_t}{Minimum distance along the ray before a hit is treated as a
crossing rather than a partition-plane boundary contact.}

\item{fail_on_error}{Logical. If \code{FALSE}, invalid or missing fits return
an NA volume with a \code{volume_log} entry instead of stopping.}
}
\value{
A volume-result list with normal-ray orientation diagnostics.
}
\description{
This is a partitioned-surface volume estimator that uses the partition plane
both to split candidate points and to determine part-wise orientation flips.
If a ray from the plane reference point intersects two surfaces in one normal
direction, the first encountered part is treated as orientation-reversed.
}
