% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface_preprocessing.R
\name{partition_by_midplane}
\alias{partition_by_midplane}
\title{Partition Surface Points by a Midplane}
\usage{
partition_by_midplane(
  surface_points,
  midplane,
  coord_cols = c("x", "y", "z"),
  overlap_prop = 0.15
)
}
\arguments{
\item{surface_points}{Data frame or matrix containing 3D points.}

\item{midplane}{Output from `estimate_midplane()`.}

\item{coord_cols}{Coordinate columns to use when `surface_points` is a data
frame.}

\item{overlap_prop}{Non-negative proportion of the largest absolute signed
distance from points to the plane. For example, `0.10` uses an overlap band
with half-width 10 percent of `max(abs(signed_distance))`.}
}
\value{
A data frame with `signed_distance`, `core_partition`, `in_overlap`,
  `partition1`, and `partition2` columns added.
}
\description{
Add partition indicators to surface points using signed distance to an
estimated midplane. Points inside the overlap band are assigned to both
partitions.
}
