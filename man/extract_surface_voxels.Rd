% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface_preprocessing.R
\name{extract_surface_voxels}
\alias{extract_surface_voxels}
\title{Extract Surface Voxels From a 3D Label Array}
\usage{
extract_surface_voxels(
  label_space,
  label_values = NULL,
  background = 0,
  connectivity = 6,
  output = c("coords", "mask"),
  voxel_spacing = NULL,
  separate_labels = FALSE
)
}
\arguments{
\item{label_space}{A 3D array containing ROI labels or a binary mask.}

\item{label_values}{Optional vector of labels to keep. If `NULL`, all values
not equal to `background` are treated as ROI.}

\item{background}{Background value. Defaults to `0`.}

\item{connectivity}{Neighbor definition: `6` for face neighbors or `26` for
all face/edge/corner neighbors.}

\item{output}{Either `"coords"` for a data frame of surface coordinates or
`"mask"` for a logical 3D surface mask.}

\item{voxel_spacing}{Optional numeric length-3 vector. When provided with
`output = "coords"`, `x_mm`, `y_mm`, and `z_mm` columns are added.}

\item{separate_labels}{If `TRUE`, surface voxels are extracted for each label
separately and then combined. This prevents adjacent labels from being
treated as one merged object.}
}
\value{
A data frame of surface voxel coordinates or a logical 3D mask.
}
\description{
A voxel is treated as a surface voxel when it belongs to the target ROI and
at least one selected neighboring voxel is background. Use 6-connectivity for
face neighbors or 26-connectivity for face, edge, and corner neighbors.
}
